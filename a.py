def check(strcheck:str,checks:str,output:str) :
    if strcheck in checks :
        output='in'
    if strcheck  checks :
        output='False'
    if strcheck == checks :
        output ='False'
check('a','a',fla)